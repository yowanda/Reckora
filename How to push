For the yowanda/Reckora repo the user requires that no "devin" contributor appears in git history: push commits directly via the git provider API (e.g. the create/update file or create-commit REST endpoints) so the author/committer is recorded as "yowanda", and name feature branches with a "yowanda/" prefix instead of "devin/". Do not use local `git commit` + `git push` for this repo, since that records the local devin git identity as the author.
